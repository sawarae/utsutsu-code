name: mascot
description: "Desktop mascot with lip-sync animation for AI coding assistants."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  utsutsu2d:
    git:
      url: https://github.com/sawarae/utsutsu2d
      ref: v0.04
  path: ^1.8.0
  screen_retriever: ^0.1.9
  window_manager: ^0.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
